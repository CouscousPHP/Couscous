language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - composer install --no-progress
  - npm install -g less
  - npm install -g less-plugin-clean-css

script:
  - phpunit

after_script:
  - bin/couscous deploy
